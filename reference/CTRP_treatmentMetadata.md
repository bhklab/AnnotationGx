# CTRP_treatmentMetadata is some preprocessed treatment metadata from the CTRP dataset

CTRP_treatmentMetadata is some preprocessed treatment metadata from the
CTRP dataset

## Usage

``` r
CTRP_treatmentMetadata
```

## Format

An object of class `data.table` (inherits from `data.frame`) with 545
rows and 2 columns.
