# CCLE_treatmentMetadata is some preprocessed treatment metadata from the CCLE dataset

CCLE_treatmentMetadata is some preprocessed treatment metadata from the
CCLE dataset

## Usage

``` r
CCLE_treatmentMetadata
```

## Format

An object of class `data.table` (inherits from `data.frame`) with 24
rows and 1 columns.
