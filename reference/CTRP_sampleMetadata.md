# CTRP_sampleMetadata is some preprocessed sample metadata from the CTRP dataset

CTRP_sampleMetadata is some preprocessed sample metadata from the CTRP
dataset

## Usage

``` r
CTRP_sampleMetadata
```

## Format

An object of class `data.table` (inherits from `data.frame`) with 1107
rows and 2 columns.
