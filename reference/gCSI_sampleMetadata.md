# gCSI_sampleMetadata is some preprocessed sample metadata from the gCSI dataset

gCSI_sampleMetadata is some preprocessed sample metadata from the gCSI
dataset

## Usage

``` r
gCSI_sampleMetadata
```

## Format

An object of class `data.table` (inherits from `data.frame`) with 1350
rows and 2 columns.
