# CCLE_sampleMetadata is some preprocessed sample metadata from the CCLE dataset

CCLE_sampleMetadata is some preprocessed sample metadata from the CCLE
dataset

## Usage

``` r
CCLE_sampleMetadata
```

## Format

An object of class `data.table` (inherits from `data.frame`) with 1461
rows and 4 columns.
