# gCSI_treatmentMetadata is some preprocessed treatment metadata from the gCSI dataset

gCSI_treatmentMetadata is some preprocessed treatment metadata from the
gCSI dataset

## Usage

``` r
gCSI_treatmentMetadata
```

## Format

An object of class `data.table` (inherits from `data.frame`) with 44
rows and 2 columns.
